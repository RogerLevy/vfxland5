fast{

: ctxvar ( <name> - ) ( - sp )
    pagealign here $100 allot&erase 
    dup 16 + over ! create , does> @ @ ;

: c{ ( a - )
    $ffffff00 and >r \ sp
    r@ @ 
    r@ c@ 16 + $ff and dup 0= if drop $10 then r@ c! \ inc sp
    r> @ 16 move ;

: c} ( a - )
    $ffffff00 and >r \ sp
    r@ c@ 16 - $ff and dup 0= if drop $f0 then r> c! ;  \ dec sp

fast}

((
ctxvar pen
111. 222. pen 2!
pen c{ pen 2p? 333. 444. pen 2! pen 2p? pen c} pen 2p? cr
pen c} pen 2p? cr 555. 666. pen 2!
pen c{ pen 2p?