public

: used ( - n ) here dp - ;

private

: hunk   used cell+ allocate throw ;
: ?free  ?dup -exit free throw ;

public

: remember  ( variable - ) 
    dup @ ?free  hunk over !  dp used rot @ lplace ;

: pristine  ( variable - ) 
    @ lcount dp swap move   quit ;

